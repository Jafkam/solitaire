if (loc_array[loc_array.length - 1] === 'index.html') {
    // do something
 } else {
    // something else
 }

 let arr = arr.length - 1 // to select the last item in the array

// once the card is flipped it stays flipped